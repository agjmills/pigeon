{
  "name": "pigeon",
  "version": "0.1.0",
  "scripts": {
    "dev": "wrangler dev --config wrangler.local.toml",
    "deploy": "wrangler deploy --config wrangler.local.toml",
    "db:migrate:local": "wrangler d1 migrations apply pigeon --local --config wrangler.local.toml",
    "db:migrate:remote": "wrangler d1 migrations apply pigeon --remote --config wrangler.local.toml",
    "db:create": "wrangler d1 create pigeon"
  },
  "dependencies": {
    "hono": "^4.4.0",
    "postal-mime": "^2.3.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240925.0",
    "typescript": "^5.5.0",
    "wrangler": "^3.78.0"
  }
}
