{
  "name": "courier",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply courier --local",
    "db:migrate:remote": "wrangler d1 migrations apply courier --remote",
    "db:create": "wrangler d1 create courier"
  },
  "dependencies": {
    "hono": "^4.4.0",
    "postal-mime": "^2.3.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240925.0",
    "typescript": "^5.5.0",
    "wrangler": "^3.78.0"
  }
}
